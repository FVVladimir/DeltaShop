import { combineReducers } from 'redux'
import { conectedRouter, connectRouter } from 'connected-react-router'

import phones from 'reducers/phones'

export default history => combineReducers({ // рутовый Reducer
  phones,
  router: connectRouter(history)
})